/*
  MouseArea's SLOT [MouseArea.onClicked] calls [label.moveTo] func.
  
  func in QML is more like python, no type declaration.
  
  funcName(arg1Name,...,argnName){funcBody}
  
  when create a func, [function funcName] type declaration is needed.
*/
import QtQuick 2.3

Item{
	width: 200
	height: 200
	
	MouseArea{
		anchors.fill: parent
		onClicked: label.moveTo(mouse.x,mouse.y)
	}
	
	Text{
		id: label
		
		function moveTo(newX,newY){
			label.x = newX;
			label.y = newY;
		}
		
		text: "move me!"
	}
}
