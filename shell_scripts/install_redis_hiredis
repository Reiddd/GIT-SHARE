sudo apt update
sudo apt upgrade

sudo apt install redis-server
# type these:
# 	redis-server
# 	redis-cli
# in shell, to make sure redis is installed.

 

# hiredis for c++
wget http://labfile.oss.alyuncs.com/courses/664/hiredis-master.zip
unzip hiredis-master.zip

cd hiredis-master

make
sudo make install
sudo ldconfig /usr/local/lib
